#export // C로 노출될 구조체
struct Point {
    f32 x;
    f32 y;
}

// TSLL-C 모듈 내부에서만 사용될 구조체 (대문자 시작)
struct InternalConfig {
    i32 setting;
}

#export // C API로 노출될 함수
f32 GetDistance(Point a, Point b) {
    // ...
}

// TSLL-C 모듈 간에만 public인 함수 (C에는 노출 안 됨)
f32 InternalHelper() {
    // ...
}

// C API로 노출될 상수
#export #const i32 VERSION = 1; 

// C에는 노출되지 않을 private 변수
#const i32 secret = 42;