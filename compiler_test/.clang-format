# .clang-format

# 베이스 스타일은 원하는 것으로 설정합니다 (예: Google, LLVM, WebKit 등)
# 아래는 Google 스타일을 기반으로 합니다.
BasedOnStyle: Google
# 들여쓰기에 스페이스를 사용하도록 설정합니다.
UseTab: Never
# 들여쓰기 너비를 4칸으로 설정합니다.
IndentWidth: 4
# Tab 너비도 4칸으로 설정합니다 (UseTab: Never 이므로 사실상 IndentWidth가 중요).
TabWidth: 4
